---
import { Badge } from "@packages/ui/components/badge";
import { Sparkles, Edit, Folder, ArrowUp, Globe, Plus } from "@lucide/astro";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@packages/ui/components/card";

const stats = [
  { value: '90+', emoji: '⭐️', label: 'Integrations' },
  { value: '56%', emoji: '👨🏽‍💻', label: 'Productivity Boost' },
  { value: '24/7', emoji: '🦜', label: 'Customer Support' },
];

const features = [
  {
    title: "Generate Ideas",
    description: "Spark creativity with AI-powered content suggestions and inspiration.",
  },
  {
    title: "Improve Writing",
    description: "Enhance your text with smart editing suggestions and style refinements.",
  },
  {
    title: "Design Layouts",
    description: "Create visually appealing layouts that capture your audience's attention.",
  },
];
---

<section class="py-24">
  <div class="mx-auto w-full max-w-3xl px-6">
    <h2 class="text-foreground text-balance text-3xl font-semibold md:text-4xl">
      <span class="text-muted-foreground">Empowering Marketing teams with</span> AI-driven solutions
    </h2>
    <div class="@container mt-12 space-y-12">
      <Card variant="soft" class="relative overflow-hidden p-0 sm:col-span-2">
        <img
          src="https://images.unsplash.com/photo-1635776062043-223faf322554?q=80&w=3132&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D"
          alt=""
          class="absolute inset-0 size-full object-cover opacity-30"
        />
        <div class="relative m-auto max-w-md p-4 sm:p-12 z-10">
          <Card aria-hidden class="relative space-y-4 p-6 bg-white/80 backdrop-blur">
            <div class="w-fit">
              <Sparkles class="size-4 fill-purple-300 stroke-purple-300" />
              <p class="mt-2 line-clamp-2 text-sm">How can I optimize my neural network to reduce inference time while maintaining accuracy?</p>
              <ul role="list" class="text-muted-foreground mt-3 space-y-2 text-sm">
                {stats.map(stat => (
                  <li class="-ml-0.5 flex items-center gap-2" key={stat.label}>
                    <span>{stat.emoji}</span>
                    <span class="text-foreground font-medium">{stat.value}</span> {stat.label}
                  </li>
                ))}
              </ul>
            </div>
            <div class="bg-foreground/5 -mx-3 -mb-3 space-y-3 rounded-lg p-3">
              <div class="text-muted-foreground text-sm">Ask AI Assistant</div>
              <div class="flex justify-between">
                <div class="flex gap-2">
                  <button class="size-7 rounded-2xl bg-transparent shadow-none border border-border flex items-center justify-center">
                    <Plus class="size-4" />
                  </button>
                  <button class="size-7 rounded-2xl bg-transparent shadow-none border border-border flex items-center justify-center">
                    <Globe class="size-4" />
                  </button>
                </div>
                <button class="size-7 rounded-2xl bg-black flex items-center justify-center">
                  <ArrowUp class="stroke-white" stroke-width="3" />
                </button>
              </div>
            </div>
          </Card>
        </div>
      </Card>
      <div class="@sm:grid-cols-2 @2xl:grid-cols-3 grid gap-6">
        {features.map(feature => (
          <div class="space-y-2" key={feature.title}>
            <h3 class="text-xl font-medium">{feature.title}</h3>
            <p class="text-muted-foreground">{feature.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
