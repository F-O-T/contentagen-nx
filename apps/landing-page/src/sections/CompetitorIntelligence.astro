---
import { Card, CardContent, CardHeader } from "@packages/ui/components/card";
import { Brain, Users, Lightbulb } from "@lucide/astro";
---

<section class="bg-zinc-50 py-16 md:py-32 dark:bg-transparent">
  <div class="mx-auto max-w-2xl px-6 lg:max-w-5xl">
    <div class="mx-auto grid gap-4 lg:grid-cols-2">
      <!-- Brand Deep Learning Feature Card -->
      <div class="group relative rounded-none shadow-zinc-950/5 bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm">
        <!-- Card Decorators -->
        <span class="border-primary absolute -left-px -top-px block size-2 border-l-2 border-t-2"></span>
        <span class="border-primary absolute -right-px -top-px block size-2 border-r-2 border-t-2"></span>
        <span class="border-primary absolute -bottom-px -left-px block size-2 border-b-2 border-l-2"></span>
        <span class="border-primary absolute -bottom-px -right-px block size-2 border-b-2 border-r-2"></span>
        
        <div class="@container/card-header grid auto-rows-min grid-rows-[auto_auto] items-start gap-1.5 px-6 has-data-[slot=card-action]:grid-cols-[1fr_auto] [.border-b]:pb-6 pb-3" data-slot="card-header">
          <div class="p-6">
            <span class="text-muted-foreground flex items-center gap-2">
              <Brain class="size-4" />
              Brand Deep Learning Workflow
            </span>
            <p class="mt-8 text-2xl font-semibold">Learn about your competitors' strategies, content patterns, and market positioning.</p>
          </div>
        </div>

        <div class="relative border-t border-dashed max-sm:mb-6">
          <div
            aria-hidden
            class="absolute inset-0"
            style="background: radial-gradient(125% 125% at 50% 0%, transparent 40%, var(--color-blue-600), var(--color-white) 100%)"
          ></div>
          <div class="aspect-[76/59] p-1 px-6">
            <div class="w-full h-full bg-gradient-to-br from-purple-100 to-blue-100 rounded-lg flex items-center justify-center">
              <Brain class="w-16 h-16 text-blue-600" />
            </div>
          </div>
        </div>
      </div>

      <!-- Multi-Agent System Feature Card -->
      <div class="group relative rounded-none shadow-zinc-950/5 bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm">
        <!-- Card Decorators -->
        <span class="border-primary absolute -left-px -top-px block size-2 border-l-2 border-t-2"></span>
        <span class="border-primary absolute -right-px -top-px block size-2 border-r-2 border-t-2"></span>
        <span class="border-primary absolute -bottom-px -left-px block size-2 border-b-2 border-l-2"></span>
        <span class="border-primary absolute -bottom-px -right-px block size-2 border-b-2 border-r-2"></span>
        
        <div class="@container/card-header grid auto-rows-min grid-rows-[auto_auto] items-start gap-1.5 px-6 has-data-[slot=card-action]:grid-cols-[1fr_auto] [.border-b]:pb-6 pb-3" data-slot="card-header">
          <div class="p-6">
            <span class="text-muted-foreground flex items-center gap-2">
              <Users class="size-4" />
              Multi-Agent System
            </span>
            <p class="mt-8 text-2xl font-semibold">Deploy specialized AI agents that work together for comprehensive content strategy.</p>
          </div>
        </div>

        <div class="px-6" data-slot="card-content">
          <div class="relative max-sm:mb-6" style="mask: radial-gradient(75% 75% at 100% 50%, transparent, transparent 75%, black);">
            <div class="aspect-[76/59] overflow-hidden rounded-lg border">
              <div class="w-full h-full bg-gradient-to-br from-green-100 to-blue-100 rounded-lg flex items-center justify-center">
                <Users class="w-16 h-16 text-blue-600" />
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Automated Ideas Generation Feature Card - Full Width -->
      <div class="group relative rounded-none shadow-zinc-950/5 bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm p-6 lg:col-span-2">
        <!-- Card Decorators -->
        <span class="border-primary absolute -left-px -top-px block size-2 border-l-2 border-t-2"></span>
        <span class="border-primary absolute -right-px -top-px block size-2 border-r-2 border-t-2"></span>
        <span class="border-primary absolute -bottom-px -left-px block size-2 border-b-2 border-l-2"></span>
        <span class="border-primary absolute -bottom-px -right-px block size-2 border-b-2 border-r-2"></span>
        
        <p class="mx-auto my-6 max-w-md text-balance text-center text-2xl font-semibold">
          Automated ideas generation powered by competitive intelligence and market analysis.
        </p>

        <div class="flex justify-center gap-6 overflow-hidden">
          <!-- Research Circle -->
          <div>
            <div class="bg-gradient-to-b from-border to-transparent size-fit rounded-2xl p-px">
              <div class="bg-gradient-to-b from-background to-muted/25 relative flex aspect-square w-fit items-center -space-x-4 rounded-[15px] p-4">
                <div class="size-7 sm:size-8 rounded-full border border-primary"></div>
                <div class="size-7 sm:size-8 rounded-full border border-primary" style="background: var(--color-background); background-image: repeating-linear-gradient(-45deg, var(--color-border), var(--color-border) 1px, transparent 1px, transparent 4px);"></div>
              </div>
            </div>
            <span class="text-muted-foreground mt-1.5 block text-center text-sm">Research</span>
          </div>

          <!-- Generate Circle -->
          <div>
            <div class="bg-gradient-to-b from-border to-transparent size-fit rounded-2xl p-px">
              <div class="bg-gradient-to-b from-background to-muted/25 relative flex aspect-square w-fit items-center -space-x-4 rounded-[15px] p-4">
                <div class="size-7 sm:size-8 rounded-full border border-primary"></div>
                <div class="size-7 sm:size-8 rounded-full border border-primary" style="background: var(--color-background); background-image: repeating-linear-gradient(-45deg, var(--color-primary), var(--color-primary) 1px, transparent 1px, transparent 4px);"></div>
              </div>
            </div>
            <span class="text-muted-foreground mt-1.5 block text-center text-sm">Generate</span>
          </div>

          <!-- Execute Circle -->
          <div>
            <div class="bg-gradient-to-b from-border to-transparent size-fit rounded-2xl p-px">
              <div class="bg-gradient-to-b from-background to-muted/25 relative flex aspect-square w-fit items-center -space-x-4 rounded-[15px] p-4">
                <div class="size-7 sm:size-8 rounded-full border border-blue-500 z-1" style="background: var(--color-background); background-image: repeating-linear-gradient(-45deg, var(--color-blue-500), var(--color-blue-500) 1px, transparent 1px, transparent 4px);"></div>
                <div class="size-7 sm:size-8 rounded-full border border-primary"></div>
              </div>
            </div>
            <span class="text-muted-foreground mt-1.5 block text-center text-sm">Execute</span>
          </div>

          <!-- Optimize Circle -->
          <div class="hidden sm:block">
            <div class="bg-gradient-to-b from-border to-transparent size-fit rounded-2xl p-px">
              <div class="bg-gradient-to-b from-background to-muted/25 relative flex aspect-square w-fit items-center -space-x-4 rounded-[15px] p-4">
                <div class="size-7 sm:size-8 rounded-full border border-primary" style="background: var(--color-background); background-image: repeating-linear-gradient(-45deg, var(--color-primary), var(--color-primary) 1px, transparent 1px, transparent 4px);"></div>
                <div class="size-7 sm:size-8 rounded-full border border-primary"></div>
              </div>
            </div>
            <span class="text-muted-foreground mt-1.5 block text-center text-sm">Optimize</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>