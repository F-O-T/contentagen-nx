---
import { sdk } from "../contentagen";
import BlogPost from "../layouts/BlogPost.astro";
import type { ContentSelect } from "@contentagen/sdk";

const { slug } = Astro.params;
const post: ContentSelect = await sdk.getContentBySlug({ slug });
---
<BlogPost post={post} />
